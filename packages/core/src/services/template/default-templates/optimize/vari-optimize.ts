import { Template } from '../../types';

export const template: Template = {
  id: 'vari-optimize',
  name: '变分法优化',
  content: [
    {
      role: 'system',
      content: `请分析和拆解用户输入的任务，然后转化成变分规划的提示词，请不要修改提示词模板，只需要把任务进行拆解，填到模板空缺的地方即可。
提示词模板如下：

你将采用变分规划进行内容生成：
## 1. 内容生成任务定义
任务类型: [空缺]
目标受众: [空缺]

主要目标:  [空缺]
内容主题： [空缺]
内容约束: [空缺]
## 2. 状态空间定义
S = {
    s1: "当前主题",
    s2: "已生成的内容长度",
    s3: "目标受众特征",
    s4: "平台特性",
    s5: "时间性因素",
    ...,
    sn: [其他相关状态变量]
}

## 3. 动作空间定义
A = {
    a1: "选择下一段落主题",
    a2: "确定段落长度",
    a3: "选择写作风格",
    a4: "插入关键词或短语",
    a5: "添加多媒体元素",
    ...,
    am: [其他内容生成相关动作]
}

## 4. 变分后验设计
q(a|s) = {
    π1(a1|s): Categorical(α1),  // 主题选择分布
    π2(a2|s): TruncatedNormal(μ2, σ2, min2, max2),  // 段落长度分布
    π3(a3|s): Categorical(α3),  // 写作风格分布
    π4(a4|s): Bernoulli(p4),  // 关键词插入概率
    π5(a5|s): Categorical(α5),  // 多媒体元素选择分布
    ...,
    πm(am|s): [其他动作的分布]
}

## 5. 奖励函数设计
R(s, a, s') = w1 * 相关性得分 +
              w2 * 吸引力评分 +
              w3 * SEO表现 +
              w4 * 阅读时长估计 -
              w5 * 违反约束惩罚

其中, w1 到 w5 为权重参数。

## 6. 优化目标
最大化 ELBO = E_q[R(s,a,s')] - β * KL(q(a|s) || p(a))
其中, p(a) 是动作的先验分布, β 是KL散度的权重。

## 7. 生成过程
1. 初始化内容状态
2. 循环直到内容完成:
  - 观察当前状态 s
  - 从 q(a|s) 采样动作 a
  - 执行动作 a (生成内容片段)
  - 更新状态到 s'
  - 计算奖励 r
3. 基于累积奖励更新变分参数

## 8. 输出格式
为每个生成步骤输出:
1. 当前状态摘要
2. 选择的动作及其概率
3. 生成的内容片段
4. 估计的部分奖励

## 9. 内容多样性控制
使用熵正则化或温度参数来控制生成内容的多样性。

## 10. 适应性调整策略
[说明如何根据内容表现和外部因素动态调整生成策略]

使用说明:
1. 根据具体的内容生成任务调整状态和动作空间。
2. 仔细设计奖励函数以反映内容质量的多个方面。
3. 通过调整 β 参数来平衡探索与利用。`
    },
    {
      role: 'user',
      content: `<Task>
{{originalPrompt}}
</Task>`
    }
  ],
  metadata: {
    version: '1.0',
    lastModified: 1704067200000, // 2024-01-01 00:00:00 UTC (固定值，内置模板不可修改)
    author: 'System',
    description: '变分法优化，使用变分规划进行内容生成和优化',
    templateType: 'optimize',
    language: 'zh'
  },
  isBuiltin: true
}; 