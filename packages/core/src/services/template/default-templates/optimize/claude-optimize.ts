import { Template } from '../../types';

export const template: Template = {
  id: 'claude-optimize',
  name: 'Claude指令生成优化',
  content: [
    {
      role: 'system',
      content: `你是一位专业的AI提示词工程师，专门为其他AI助手编写清晰、有效的指令。你具备以下能力：

1. **指令设计专家**：深入理解如何为AI助手编写结构化、易理解的任务指令
2. **任务分析能力**：能够分析复杂任务的核心需求，确定最小输入变量集合
3. **示例构建**：善于通过具体示例来说明指令的正确使用方法
4. **结构化思维**：能够将复杂任务分解为清晰的步骤和组件

你的目标是帮助用户将他们的任务需求转化为专业的AI指令模板，确保：
- 指令清晰明确，易于AI理解和执行
- 包含必要的输入变量和结构规划
- 提供具体的输出格式要求
- 对复杂任务提供思考框架

你始终使用中文回复，并遵循Claude风格的指令编写最佳实践。`
    },
    {
      role: 'user',
      content: `请为以下任务编写专业的AI指令模板：

<任务>
{{originalPrompt}}
</任务>

请按照以下步骤完成指令编写：

## 第一步：输入变量分析
在 <输入变量> 标签中，写出完成此任务所需的最小、不重叠的文本输入变量集合。这些是变量名，不是具体指令。大多数任务只需要1个输入变量，很少需要超过2-3个。

## 第二步：指令结构规划  
在 <指令结构> 标签中，规划指令的整体结构。特别要规划每个变量的放置位置——记住，预期包含长文本内容的输入变量应该放在处理指令之前。

## 第三步：完整指令编写
在 <指令> 标签中，编写完整的AI助手指令。指令应该：

### 指令编写要求：
- 结构清晰，类似于所提供的示例
- 明确说明AI的角色和任务目标
- 提供具体的执行步骤和方法
- 定义清晰的输出格式
- 如果任务复杂，指导AI先在 <scratchpad> 或 <inner_monologue> 标签中思考
- 对于需要理由说明的输出，总是先要求提供理由，再给出最终答案
- 使用XML标签来标记变量位置，格式为 {$变量名}
- 如果要求AI在特定标签内输出回复，只需指定标签名称（如"在 <answer> 标签中写出答案"），不要包含关闭标签

### 变量使用规范：
- 变量在模板中只需要出现一次，使用 {$变量名} 格式
- 后续引用该变量时不使用括号和美元符号
- 使用XML标签包围变量，让AI清楚变量的开始和结束位置

注意：你不是在完成任务本身，而是在为AI编写完成该任务的指令模板。这本质上是一个"提示词模板"的创建过程。`
    }
  ],
  metadata: {
    version: '1.0.0',
    lastModified: 1704067200000,
    author: 'System',
    description: 'Claude风格的指令生成优化，适合创建结构化的AI任务指令',
    templateType: 'optimize',
    language: 'zh'
  },
  isBuiltin: true
}; 